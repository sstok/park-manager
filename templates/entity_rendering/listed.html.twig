<dl>
    {% for label, entities in sets %}
        <dt class="mt-4 font-bold">{{ ('entity_label.' ~ label)|trans }}</dt>
        <dd>
            <ul class="list-disc list-inside">
                {% for entity in entities %}
                    <li>{{ entity|raw }}</li>
                {% endfor %}
            </ul>
        </dd>
    {% endfor %}
</dl>

